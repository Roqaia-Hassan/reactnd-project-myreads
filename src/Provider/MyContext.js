import React from 'react'

// Create a context to pass the state deep into the component tree
const MyContext = React.createContext();

// Exporting the MyContext constant to be used out of its scope 
export {MyContext}
